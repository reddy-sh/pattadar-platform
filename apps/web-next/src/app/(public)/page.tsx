'use client';

// Ported from apps/web/src/pages/landing/LandingPage.tsx (task C9). Replaces
// the B5 placeholder — kept as-is per founder decision D4 (Aceternity-style
// page, not restyled onto Minimals).
import { LandingPage } from 'src/views/landing/LandingPage';

export default function Page() {
  return <LandingPage />;
}
