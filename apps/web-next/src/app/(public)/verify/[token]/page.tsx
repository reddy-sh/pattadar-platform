'use client';

// Route skeleton stub (task B5) — Phase C ports the real beneficiary
// verification landing (verifyBeneficiary mutation on the invite token).
import EmptyContent from 'src/components/empty-content';
import { PageHeader } from 'src/components/PageHeader';

export default function VerifyTokenPage() {
  return (
    <>
      <PageHeader title="Verify" />
      <EmptyContent title="Coming soon" description="This page is coming soon." filled />
    </>
  );
}
