import Typography from '@mui/material/Typography';

export function PassbooksPage() {
  return (
    <>
      <Typography variant="h2">Passbooks</Typography>
      <Typography color="text.secondary">
        Passbook list, detail, and AI passbook-upload import; rebuilt from rhub PassbooksView,
        PassbookDetailView, and PassbookCreateModal.tsx.
      </Typography>
    </>
  );
}
